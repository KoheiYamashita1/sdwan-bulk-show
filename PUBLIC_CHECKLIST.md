# Public Release Checklist

- Remove sensitive data from the repo history (IPs, usernames, passwords, hostnames).
- Ensure `host.txt`, `command.txt`, and `logs/` are excluded from Git.
- Replace real values in README examples with placeholders.
- Verify no private keys or SSH config files are present.
- Confirm Python version requirements and dependencies are documented.
- Add a LICENSE file.
